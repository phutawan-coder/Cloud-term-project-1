stages:
  - build
  - deploy

variables:
  DOCKER_IMAGE: file-upload-app

build:
  stage: build
  script:
    - docker build -t $DOCKER_IMAGE .

deploy:
  stage: deploy
  script:
    - terraform init
    - terraform apply -auto-approve
